import { InAppListComponent } from "./InAppList";
import { InAppListSkeleton } from "./InAppListSkeleton";

export const InAppList = Object.assign(InAppListComponent, {
  Skeleton: InAppListSkeleton,
});
